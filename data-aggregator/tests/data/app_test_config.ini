[DEFAULT]

DATA_DIR = /for/test/only
OUTPUT_DIR = /for/test/only

OUTPUT_LOG = test.log

# to test boolean type conversion
LOG_DISABLED = True

# DO NOT add LOG_FORMAT here: we use it to test resolving from settings

# to test loading a key not defined in settings
NOT_IN_SETTINGS = some text

# to test python evaluation (monkeypatch to declare this in settings.config_access)
SOME_FANCY_EXPRESSION = set(['fancy', 'python', 'expression'])
